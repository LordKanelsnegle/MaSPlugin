package gg.playtogether.mc.commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

public class CommandPull implements CommandExecutor {
    @Override
    public boolean onCommand(CommandSender sender, Command command, String label, String[] args) {
        // check if windows or linux
            // set path and file extension (.bat vs .sh)
        // execute the file
        // return the response, green for success or white for no new updates, red for fail
        return true;
    }
}
 